import { AvailableLocales } from "./types";

const availableLocales: AvailableLocales = {
  pt: "pt",
  en: "en",
};

const availableLocalesList = Object.keys(availableLocales);

export { availableLocales, availableLocalesList };
