type PopoverParamsType = React.PropsWithChildren & { local?: "top" | "bottom" };

type WrapperStyleParamsType = { $top: string; $bottom: string };

export type { PopoverParamsType, WrapperStyleParamsType };
