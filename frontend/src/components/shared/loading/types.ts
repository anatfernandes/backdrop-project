type WrapperPropsType = {
  $margin: string;
};

type LoadingParamsType = {
  size?: "normal" | "small" | "large";
  color?: string;
  margin?: string;
};

export type { LoadingParamsType, WrapperPropsType };
